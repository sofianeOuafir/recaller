<% if @support.errors.present? %>
  $('#modal').remove();
  $("#modal-container").html("<%= escape_javascript(render(partial:'supports/new', locals:{support: @support})).html_safe %>");
  $('#modal').modal();
<% else %>
  location.reload();
<% end %>
