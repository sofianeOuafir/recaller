$('#modal').remove();
$("#modal-container").html("<%= escape_javascript(render(partial:'reviews/show', locals:{review: @review})).html_safe %>");
$('#modal').modal({
  escapeClose: false,
  clickClose: false,
  showClose: false
  }
);

