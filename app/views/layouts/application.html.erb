<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= yield :head %>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <div class="container-fluid main-container" style="border:1px black solid">
        <div class="row" style="border:1px black solid">
            <div class="col-sm-offset-2 col-sm-3" style="border:1px red solid;">
                <%= render partial: 'shared/title', locals: {title: "Medias", title_tag_is_defined: false} %>
                <%= render @supports %>
                <div class="row">
                    <%= link_to new_support_path do %>
                        <span>Nouveau media</span>
                    <% end %>
                </div>
            </div>
            <div class="col-sm-5" style="border:1px black solid;">
                <%= yield %>
            </div>
        </div>
    </div>
  </body>
</html>
