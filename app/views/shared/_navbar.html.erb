<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
        <%= link_to "Recall", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar">
      <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to 'Home', root_path, class: "navbar-text-color" %></li>
      </ul>
      <% end %>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle navbar-text-color" data-toggle="dropdown" href="#">
              <%= current_user.username %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Mon profil', edit_user_registration_path %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Se connecter', new_user_session_path, class: "navbar-text-color" %></li>
          <li><%= link_to 'S\'inscrire', new_user_registration_path, class: "navbar-text-color" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>