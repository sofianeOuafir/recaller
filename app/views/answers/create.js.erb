$('#modal').remove();
$("#modal-container").html("<%= escape_javascript(render(partial:'answers/show', locals:{answer: @answer})).html_safe %>");
$('#modal').modal({
  escapeClose: false,
  clickClose: false,
  showClose: false
  }
);

