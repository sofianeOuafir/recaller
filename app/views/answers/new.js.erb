$('#modal').remove();
$("#modal-container").html("<%= escape_javascript(render(partial:'answers/new', locals:{question: @question})).html_safe %>");
$('#modal').modal({
  escapeClose: false,
  clickClose: false,
  showClose: false
  }
);

