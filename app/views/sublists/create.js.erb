<% if @topic.errors.present? %>
  $('#modal').remove();
  $("#modal-container").html("<%= escape_javascript(render(partial:'sublists/new', locals:{support: @support, topic: @topic, translations: @translations})).html_safe %>");
  $('#modal').modal();
<% else %>
  location.reload();
<% end %>
